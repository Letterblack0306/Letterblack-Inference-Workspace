# Examples

Declarative extension manifests, endpoint definitions, action definitions, model sidecars, and launch-profile examples.

Examples must be clearly labeled and must not be loaded as live operational state by default.
